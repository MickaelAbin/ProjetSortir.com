{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="../{{ asset('css/CarteCss.css') }}">
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('Script/Lieu_Adresse.js') }}" defer></script>
    <script src="{{ asset('Script/Creation_Lieu.js') }}" defer ></script>
{% endblock %}

{% block title %}Ajouter un lieu{% endblock %}

{% block body %}
<h2>Ajouter votre Lieu !</h2>

{#    {{ form_start(lieuForm) }}#}
{#    {{ form_widget(lieuForm) }}#}
{#    <a href="{{ path('ville_create') }}"><img src="../{{ asset('Images/Plus.png') }}" alt="un plus pour ajouter un lieu"></a>#}
{#    {{ form_end(lieuForm) }}#}
    <section>
        <article>

            <div class="adresse">
                <label for="adresse">Adresse : </label>
                <input type="search" list="listeAdresse" id="adresse" name="adresse" value="">

                <label for="codePostal">Code postal : </label>
                <input type="search" list="listeCodes" id="codePostal" name="codePostal" value="">

                <input type="submit" id="recherche" value="Rechercher">

                <datalist id="listeAdresse"></datalist>
                <datalist id="listeCodes"></datalist>
            </div>

            {{ form_start(lieuForm) }}
                {{ form_widget(lieuForm) }}
                <button>Ajouter</button>
            {{ form_end(lieuForm) }}

        </article>

        <div id="map"></div>
    </section>

{% endblock %}
