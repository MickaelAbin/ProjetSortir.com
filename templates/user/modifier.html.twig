{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mon profil</h1>

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.pseudo) }}
    {{ form_row(registrationForm.prenom) }}
    {{ form_row(registrationForm.nom) }}
    {{ form_row(registrationForm.telephone) }}
    {{ form_row(registrationForm.email) }}
    {{ form_row(registrationForm.plainPassword) }}
    {{ form_row(registrationForm.site) }}
    {{ form_row(registrationForm.imageFile) }}
    <button type="submit" class="button is-primary is-outlined">Enregistrer</button>
    {{ form_end(registrationForm) }}
    <div class="retour">
        <a href="{{ path('sortie_index') }}"><button class="button is-primary is-outlined"> Annuler</button></a>
    </div>
{% endblock %}

{% block title %}

{% endblock %}

