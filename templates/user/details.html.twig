{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
<h1>{{ user.pseudo }}</h1>
    Prénom : <div>{{ user.prenom }}</div>
    Nom : <div>{{ user.nom }}</div>
    Téléphone : <div>{{ user.telephone }}</div>
    Email : <div>{{ user.email }}</div>
    Ville de rattachement : <div>{{ user.site.nom_site }}</div>

    <button > <a href="{{ path('sortie_detail', {'id':sortie.id}) }}">Retour</a></button>

{% endblock %}

